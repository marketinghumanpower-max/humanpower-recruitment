/**
 * Feature: candidates
 *
 * This is an example feature module following the project conventions.
 * Each feature can contain its own components/, hooks/, and api/ sub-folders.
 *
 * Structure:
 *   features/candidates/
 *   ├── components/    # UI components specific to this feature
 *   ├── hooks/         # TanStack Query hooks (useXxxQuery, useXxxMutation)
 *   └── api/           # API functions specific to this feature (optional)
 */
